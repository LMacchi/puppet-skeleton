fixtures:
  repositories:
    stdlib: "https://github.com/puppetlabs/puppetlabs-stdlib.git"
  symlinks:
    <%= metadata.name %>: "#{source_dir}"
    <%= metadata.name %>/files: '../../../../files'
    <%= metadata.name %>/manifests: '../../../../manifests'
    <%= metadata.name %>/templates: '../../../../templates'
